# 🌐 CAICT Agent Trust Handshake (ATH 1.0) Specification & Cross-Jurisdiction Mapping
# 中国信通院 智能体可信握手协议 (ATH 1.0) 互操作规范

---

## 1. Overview: The 2026 Sovereign Standards Convergence

On May 7, 2026, the **China Academy of Information and Communications Technology (CAICT)**, Tencent, and Huawei open-sourced the **Agent Trust Handshake (ATH 1.0)** protocol specification, aligning with the CAC, NDRC, and MIIT *Implementation Opinions on Promoting the High-Quality Development of Agentic AI Infrastructure*.

ATH 1.0 establishes that autonomous agents acting across organizational boundaries cannot rely on bilateral API keys or informal prompt instructions. It mandates a **three-party (User – Agent – Service) 9-step decentralized verification handshake** and non-repudiable runtime behavior tracing.

`smaos-audit` and `aeib-receipt-fuzzer` provide the world's only zero-egress translation layer bridging **EU DORA RTS 2024/1772**, **US NIST RMF (SP 800-53 Rev. 5)**, and **China CAICT ATH 1.0**.

---

## 2. The 9-Step ATH 1.0 Lifecycle vs. SMAOS Disposition Lattice

```text
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                        CAICT ATH 1.0 DECENTRALIZED TRUST HANDSHAKE                     │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ 1. Identity Assert (DID) ─────────> 2. Mutual TLS Handshake ─────> 3. Intent Card Hash │
│ 4. Capability Envelope Match ────> 5. Authorization Token ───────> 6. Tool Invocation  │
│ 7. Pre-Commit Wire Ack ───────────> 8. State Settlement ─────────> 9. Attestation Close│
└────────────────────────────────────────────────────────────────────────────────────────┘
```

### Tripartite Disposition Translation Matrix

| SMAOS Disposition | CAICT ATH 1.0 State | EU DORA RTS 2024/1772 | US NIST SP 800-53 Rev. 5 |
| :--- | :--- | :--- | :--- |
| **`CONFIRMED`** | Steps 1–9 Complete (`SETTLED`) | Cleared Nominal Settlement | SCITT Confirmed State Change (`AC-3`) |
| **`UNKNOWN`** | Post-Step 6 Wire Drop (`SERVICE_UNOBSERVABLE`) | **DORA Art. 17 Major Incident** (4h clock) | **CP-10 / IR-4**: Non-Repudiable Halt |
| **`CONFLICT`** | Dual-Handshake Hash Mismatch (`PHASE_DRIFT`) | Settlement Drift / Double-Spend | System Integrity Violation (`SI-7`) |
| **`INVALID_INPUT`** | Schema Tampering / Rug Pull (`SCHEMA_REJECT`) | Unauthorized MCP Expansion | Information Input Validation (`SI-10`) |
| **`REFUSED`** | Capability Policy Reject (`POLICY_DENIED`) | Access Control Policy Halt | Least Privilege Access Halt (`AC-6`) |
| **`MISSING_EVIDENCE`**| Step 9 Attestation Missing (`UNATTESTED`) | Incomplete Audit Trail | Audit Record Generation Gap (`AU-2`) |

---

## 3. Bilingual Compliance Schema (JSON Attestation)

Execution receipts generated by the SMAOS engine natively contain the dual-jurisdiction tags:

```json
{
  "attestation_version": "v1.1.0",
  "disposition": "UNKNOWN",
  "regulatory_mapping": {
    "eu_dora": "RTS_2024_1772_ARTICLE_17_MAJOR_INCIDENT",
    "china_caict_ath": "ATH_1_0_STEP_7_SERVICE_UNOBSERVABLE",
    "us_nist": "SP_800_53_CP_10_CONTINGENCY_HOLD"
  },
  "wire_observer": {
    "transport_fault": "HTTP_504_GATEWAY_TIMEOUT",
    "pii_scrubbed": true,
    "crypto_suite": "Ed25519+ML-DSA-65"
  }
}
```
